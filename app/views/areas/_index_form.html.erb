<div class="table-responsive">
  <table class="table table-responsive table-condensed table-hover table-striped">
    <thead>
      <tr>
        <th><%=Area.human_attribute_name(:id)%></th>
        <th><%=Area.human_attribute_name(:customer_company_id)%></th>
        <th><%=Area.human_attribute_name(:area)%></th>
        <th><%=Area.human_attribute_name(:order)%></th>
        <th><%=Area.human_attribute_name(:area_id)%></th>
        <th><%=Area.human_attribute_name(:active)%></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @areas.each do |area| %>
        <tr>
          <td><%= area.id %></td>
          <td>
            <% unless area.customer_company.nil? %>
                <%= area.customer_company.company_customer %>
            <% end %>
          </td>
          <td><%= area.area %></td>
          <td><%= area.order %></td>
          <td>
            <% unless area.up_area.nil? %>
              <%= area.up_area.area %>
            <% end %>
          </td>
          <td><%= t(area.active_desc) %></td>
          <td>
            <%= link_to t('helpers.submit.show'), area, class: "btn btn-default" %>
            <%= link_to t('helpers.submit.edit'), edit_area_path(area), class: "btn btn-success" %>
            <%= link_to t('helpers.submit.delete'), area, class: "btn btn-danger", method: :delete, data: { confirm: t('messages.confirm_are_you_sure') } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

