<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">
        <%= image_tag("logo-planeacion-plano.png", size: "20x20") %>
      </a>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <% unless current_user.blank? %>
            <p class="navbar-text navbar-link"><%= t('views.authentication.user') %> <strong><%= current_user.email %> </strong></p>
          <% end %>
        </li>
        <li>
          <% unless current_user.blank? %>
            <%= link_to t('views.buttons.logout'), destroy_user_session_path, method: :delete, :class => 'label label-danger' %>
            <%#= link_to "Crear Cuenta", new_user_registration_path, :class => 'navbar-brand'  %>
            <%#= link_to "Iniciar Sesión", new_user_session_path, :class => 'navbar-brand'  %>
          <% end %>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= t("views.navigation.language") %><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to("Español", "#{Rails.application.config.spanish_website_url}#{request.env['PATH_INFO']}") %></li>
            <li><%= link_to("English", "#{Rails.application.config.english_website_url}#{request.env['PATH_INFO']}") %></li>
            <li><%= link_to("Português", "#{Rails.application.config.portuguese_website_url}#{request.env['PATH_INFO']}") %></li>
          </ul>
        </li>
      </ul>



    </div> <!-- /.navbar-collapse -->
  </div> <!-- /.container-fluid -->
</nav>
